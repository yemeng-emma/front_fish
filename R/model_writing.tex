% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
%
\documentclass[
]{article}
\usepackage{amsmath,amssymb}
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\ifLuaTeX
  \usepackage{selnolig}  % disable illegal ligatures
\fi
\IfFileExists{bookmark.sty}{\usepackage{bookmark}}{\usepackage{hyperref}}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={model\_writing},
  pdfauthor={Meng Ye},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{model\_writing}
\author{Meng Ye}
\date{2025-11-12}

\begin{document}
\maketitle

\[
\begin{aligned}
&\ \textbf{Multinomial probability of selection of choice}_{i}\ \textbf{in respondent}_{j} \\
\text{Choice}_{i_j} \sim&\ \operatorname{Categorical}\big(\{\mu_{1,i_j},\,\mu_{2,i_j},\,\mu_{3,i_j}\}\big) \\[10pt]
&\ \textbf{Model for probability of each option} \\
\{\mu_{1,i_j},\,\mu_{2,i_j},\,\mu_{3,i_j}\} =&\ (\beta_0 + b_{0_j}) \\
&\ + \beta_1\,\text{rbi[Reduced price]}_{i_j}
  + \beta_2\,\text{rbi[Full price]}_{i_j} \\
&\ + \beta_3\,\text{ubi[Event space rental]}_{i_j}
  + \beta_4\,\text{ubi[Running a pizza shop]}_{i_j} \\
&\ + \beta_5\,\text{nonprofit type[Student tutoring center]}_{i_j}
  + \beta_6\,\text{nonprofit type[Urban forestry center]}_{i_j}
  + \beta_7\,\text{nonprofit type[Mental health center]}_{i_j} \\
&\ + \beta_8\,\text{use of donations[Capacity building]}_{i_j}
  + \beta_9\,\text{use of donations[Rainy day funds]}_{i_j}
  + \beta_{10}\,\text{use of donations[Unrestricted use]}_{i_j} \\
&\ + \beta_{11}\,\text{financial situation[Under financial stress]}_{i_j} \\[6pt]
b_{0_j} \sim&\ \mathcal{N}(0,\sigma_0)\quad \text{Respondent-specific offset from global probability} \\[10pt]
&\ \textbf{Priors} \\
\beta_{0\dots 11} \sim&\ \mathcal{N}(0,3) \\
\sigma_0 \sim&\ \operatorname{Exponential}(1)
\end{aligned}
\] Testing a narrower arrangement

\$\$

\begin{aligned}
&\ \textbf{Multinomial probability of selection of choice}_{i}
   \ \textbf{in respondent}_{j} \\[4pt]
\text{Choice}_{i_j} \sim&
   \ \operatorname{Categorical}\big(\{\mu_{1,i_j},\,\mu_{2,i_j},\,\mu_{3,i_j}\}\big) \\[10pt]

&\ \textbf{Model for probability of each option} \\[3pt]
\{\mu_{1,i_j},\,\mu_{2,i_j},\,\mu_{3,i_j}\} =&\
   (\beta_0 + b_{0_j}) \\[3pt]
&\ + \beta_1\,\text{rbi[Reduced price]}_{i_j}
  + \beta_2\,\text{rbi[Full price]}_{i_j} \\[3pt]
&\ + \beta_3\,\text{ubi[Event space rental]}_{i_j}
  + \beta_4\,\text{ubi[Running a pizza shop]}_{i_j} \\[3pt]
&\ + \beta_5\,\text{nonprofit type[Student tutoring center]}_{i_j}
  + \beta_6\,\text{nonprofit type[Urban forestry center]}_{i_j} \\[3pt]
&\ + \beta_7\,\text{nonprofit type[Mental health center]}_{i_j} \\[3pt]
&\ + \beta_8\,\text{use of donations[Capacity building]}_{i_j}
  + \beta_9\,\text{use of donations[Rainy day funds]}_{i_j} \\[3pt]
&\ + \beta_{10}\,\text{use of donations[Unrestricted use]}_{i_j} \\[3pt]
&\ + \beta_{11}\,\text{financial situation[Under financial stress]}_{i_j} \\[8pt]

b_{0_j} \sim&\ \mathcal{N}(0,\sigma_0)
   \quad \text{Respondent-specific offset from global probability} \\[10pt]

&\ \textbf{Priors} \\[3pt]
\beta_{0\dots 11} \sim&\ \mathcal{N}(0,3)
   \qquad
\sigma_0 \sim\ \operatorname{Exponential}(1)
\end{aligned}

~\operatorname{Exponential}(1) \textbackslash end\{aligned\} \$\$

\end{document}
